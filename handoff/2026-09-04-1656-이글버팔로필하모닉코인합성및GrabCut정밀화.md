# 이글/버팔로/필하모닉 코인 합성 및 GrabCut 정밀화 인수인계 문서

> **커밋 로그 기반 사후 작성** — 지난 세션에서 인수인계 문서를 작성하지 못해,
> 이번 문서는 대화 기록이 아닌 `git log` / `git show` (diff 포함) 만을 근거로
> 사후 재구성했다. "확인 완료" 항목은 커밋 diff·메시지로 직접 검증한 내용이고,
> "미확인" 항목은 실제 세션에서 어떤 논의·검증이 있었는지 알 수 없어 커밋
> 메시지의 서술을 그대로 신뢰할 수밖에 없는 부분이다.

작성일: 2026-09-04

> **나중에 이 작업을 다시 찾을 때 검색 키워드**: "이글 버팔로 필하모닉 코인 합성",
> "GrabCut 마스크"

## 1. 목표

직전 핸드오프(`2026-09-04-1552-캥거루코인합성.md`)에서 캥거루 슬라이드에 시범
적용했던 "배경/테두리는 유지하고 코인 얼굴만 실제 2026년 판매 상품 디자인으로
합성 교체" 작업을 나머지 3개 히어로 슬라이드(이글/버팔로/필하모닉)에도 적용하고,
그 과정에서 발견된 마스크 부정확 문제(코인 테두리가 새 디자인 밖으로 삐져나오는
현상)를 해결하기 위해 합성 방식 자체를 개선하는 것.

## 2. 현재 상태

**확인 완료 (커밋 diff·메시지로 직접 검증)**:

- 커밋 `f4c81ad` (2026-09-04 16:24 KST): `images/header-slide-eagle.jpg`,
  `header-slide-buffalo.jpg`, `header-slide-philharmonic.jpg` 3개 파일 교체.
  커밋 메시지에 "캥거루와 동일한 방식(알파 컷아웃 + 색보정 + 페더링)으로 코인
  얼굴을 실제 판매 상품 이미지로 교체. 배경(사막/초원/알프스)과 코인 구도는
  원본 유지"라고 명시되어 있음. 코드 변경 없이 바이너리 이미지 파일만 변경됨
  (3 files changed, 0 insertions/deletions — 텍스트 diff 없음).
- 커밋 `2efa23d` (2026-09-04 16:43 KST): 위 3개 + `header-slide-kangaroo.jpg`
  까지 총 4개 파일을 GrabCut 기반 방식으로 재작업. 커밋 메시지 근거:
  - 기존 "눈대중 타원 피팅" 방식이 코인 형태 추정을 부정확하게 해, 이글·버팔로는
    심하게, 캥거루·필하모닉은 약간 원본 코인 테두리가 새 디자인 밖으로 노출되는
    문제가 있었다고 기록됨.
  - OpenCV GrabCut으로 원본 사진에서 실제 코인 실루엣을 정밀 추출 → 모폴로지
    open + 타원 피팅으로 마스크를 정리하는 방식으로 변경.
  - 색보정 샘플링을 median 기반으로 바꾸고 gain 클리핑 범위를 0.75~1.3으로
    축소(필하모닉이 과다 보정으로 하얗게 뜨던 문제 대응).
  - 이 역시 텍스트 diff 없이 바이너리 이미지 파일만 변경(4 files changed,
    0 insertions/deletions).
- `git log` 기준 두 커밋 모두 작성자 `Claude <claude@anthropic.com>`이며, 사이에
  다른 코드 변경 커밋은 없음(자동 생성 봇 커밋 제외 시 두 커밋만 존재).
- 현재 `main` 브랜치 HEAD는 `2efa23d`이고 원격과 동기화된 상태이며 워킹 트리는
  clean함(이번 세션 시작 시 재확인).

**미확인 (기록만으로는 판단 불가)**:

- 실제 배포된 GitHub Pages 사이트에서 4개 슬라이드가 브라우저상 자연스럽게
  보이는지 확인되지 않음. 직전 캥거루 핸드오프에서도 "배포 사이트 렌더링 확인
  안 됨"이 미해결 항목으로 남아 있었는데, 이번 두 커밋의 커밋 메시지에도 배포
  확인에 대한 언급이 없어 여전히 미확인 상태로 추정됨(단, 세션 중 실제로
  확인했으나 커밋 메시지에만 안 적었을 가능성도 배제할 수 없음 — 대화 기록이
  없어 단정 불가).
- 캥거루 작업 때는 "`present_files`로 원본/새 버전을 모두 사용자에게 전달 후
  승인받는다"는 절차가 핸드오프에 필수 단계로 명시되어 있었음. 이번 두 커밋에
  대해서도 동일하게 사용자 승인을 받았는지는 커밋 로그만으로는 알 수 없음
  (승인 없이 바로 커밋했을 가능성도 있음).
- GrabCut 재작업의 계기가 된 "테두리 노출 문제"를 누가 발견했는지(Claude 자체
  검수 중 발견 vs 사용자 피드백) 불명.
- 이미지가 바이너리 파일이라 diff로는 실제 화질·합성 결과를 확인할 수 없음.
  실제 결과물을 보려면 `images/header-slide-*.jpg`를 직접 열어봐야 함.
- 백업 브랜치 확인 결과, 이번 세션(2026-09-04 16시대) 시점으로 생성된 백업
  브랜치는 없음(원격에 남아있는 가장 최근 백업은 `backup-2026-08-20-1755`).
  이미지 파일 교체는 git 히스토리로 되돌리기 쉬워 백업 브랜치 없이 진행한
  것으로 보이나, 이 판단이 실제로 세션 중 의도된 것인지는 불명.

## 3. 진행 단계

이번 문서는 사후 재구성이므로 "진행 단계"는 재현 가능한 절차가 아니라, 커밋
메시지에서 추론한 **실제 진행 순서 요약**이다.

1. (완료) 캥거루 방식(알파 컷아웃 + 색보정 게인 + 페더링, 눈대중 타원 피팅)을
   이글/버팔로/필하모닉 3개에 그대로 적용 → 커밋 `f4c81ad`.
2. (완료) 결과 검수 중 이글·버팔로(심하게), 캥거루·필하모닉(약간)에서 원본
   코인 테두리가 새 디자인 밖으로 삐져나오는 문제 발견.
3. (완료) 눈대중 타원 피팅 대신 OpenCV GrabCut으로 마스크를 정밀 추출하는
   방식으로 전환, 모폴로지 open + 타원 피팅으로 마스크 후처리 추가.
4. (완료) 색보정 샘플링을 median 기반으로 변경, gain 클리핑 범위를 0.75~1.3으로
   축소.
5. (완료) 4개 슬라이드(이글/버팔로/필하모닉/캥거루) 전체 재작업 → 커밋 `2efa23d`.
6. (미확인) 사용자 승인 및 배포 사이트 확인 — 위 "미확인" 항목 참조.

## 4. 다음에 진행할 일 (To-Do)

직전 핸드오프(`2026-09-04-1552`)의 To-Do를 기준으로, 이번 세션 커밋 내용을
반영해 갱신함. 캥거루 슬라이드 관련 항목은 이번 GrabCut 재작업으로 대체되어
통합했고, 나머지 항목은 명시적 종결 기록이 없어 그대로 이월함.

### 🎨 UI/UX
- 🔴 👤 **배포된 사이트에서 4개 히어로 슬라이드(이글/버팔로/필하모닉/캥거루) 실제
  렌더링 확인** — GrabCut 재작업 결과가 브라우저에서도 테두리 노출 없이 자연스러운지
  (기존 "캥거루만" 항목에서 4개 전체로 범위 확장, 미확인 상태)
- 🔴 👤 이번 두 커밋(`f4c81ad`, `2efa23d`)의 합성 결과물에 대해 사용자 승인을
  받았는지 확인 — 안 받았다면 원본/새 버전 비교 이미지를 다시 전달받아 검토 필요
- 🔴 👤 **다음 세션 최우선(기존 항목, 이월)**: 코인 상세 페이지("구매 문의" 페이지,
  `coin-detail.html` + `coins/*.html` 30개)의 "구매안내" 탭을 신뢰 섹션으로
  교체하는 안 — 보류 중이던 선택지 재검토:
  - A. 완전 교체(FAQ/가격안내/구매절차/배송·환불 정보 삭제)
  - B. 기존 정보(FAQ/배송·환불 등)는 "상품정보" 탭 등으로 이동 후, "구매안내"
    탭 자리만 신뢰 섹션으로 교체
  - 결정 시 `coin-detail.html`(클라이언트 렌더링 폴백)과
    `scripts/generate-coin-pages.js`(30개 정적 페이지 생성 템플릿) 두 곳 모두
    수정 필요
- 🟡 👤 첫 번째 영상 슬라이드 교체용 새 영상 준비 → 완료 시 🤖 `index.html`
  `heroSlides` 배열의 주석 해제 또는 `video` 경로 교체 (기존 항목, 이월)
- 🔴 🤖 카테고리 배너 톤 15/16/17안(미니멀) 피드백 — 여러 세션째 대기 중
  (기존 항목, 이월)
- 🟡 🤖 Royal Mint, Perth Mint 등 해외 참고 배너 이미지 추가 검색 (원하시면)
- 🔵 👤 미사용 배너 SVG 파일 정리 여부 결정 — 톤 확정 후 일괄 정리

### 🔍 SEO
- 🔴 👤 재제출한 sitemap.xml 처리 상태 재확인 (2026.8.3 재제출 기준, 기존 항목 이월)
- 📢 👤 서치콘솔 오류 문구·크롤링 통계 추가 캡처해서 전달

### 🛍️ 상품 데이터
- 🔵 👤 정식 사업장 주소(소호사무실/사물함형 임대) — 실수요 발생 시 진행
- ⚪ 👤 소셜 미디어 계정 확정 → 🤖 footer.js 반영
- 🔵 👤 통신판매업신고번호 신청 여부 재검토 (거래량 50회 근접 시)
- 🔵 👤 구매안내 탭 문구(결제/배송/교환·환불) 실제 정책 검토 후 → 🤖 반영
- ⚪ 👤 상호 변경(골든민트 → 원트로이불리온) 신고 진행 여부
- ⚪ 👤 전화번호 010-3737-5757 "(임시)" 상태 유지 중

### ⚙️ 기능/인프라
- 🟡 👤 GA4 실시간 데이터 수집 확인 (모바일)
- 🟡 👤 간편 문의 폼(구글폼) — 폼 생성 후 URL 전달 → 🤖 연동
- 🟡 👤 비밀번호 찾기(재설정) 기능 추가 여부 결정 → 🤖 반영
- 🔵 👤 favicon .ico / apple-touch-icon 추가
- 🔵 👤 네이버 톡톡 도입 여부 결정
- 🔵 👤 이메일 인증 도입 여부 결정

### 📋 운영/프로세스
- 🟡 👤 onetroybullion.com / .co.kr 도메인 구매
- 🔴 🤖 배너 시안 12개 연속 반려됨 — 다음 라운드도 반려 시 접근 방식 자체
  재검토 필요 (기존 항목, 이월)
- 🟡 👤 세션 종료 전 인수인계 문서 작성을 잊지 않도록 유의 — 이번 문서가
  "사후 작성"이 된 원인 자체가 To-Do 항목은 아니지만, 반복되지 않도록 각주로 남김

## 5. 참고 사항

- **이 문서의 한계**: 대화 로그가 없어 사용자 피드백, 논의 과정, 실제 승인
  여부를 알 수 없다. 위 "미확인" 항목들은 다음 세션에서 직접 확인이 필요하다.
- **이미지 바이너리 특성**: `images/header-slide-*.jpg` 4개 파일은 git diff로
  내용 검증이 불가능하므로, 결과를 확인하려면 반드시 이미지를 직접 열어보거나
  배포된 사이트를 방문해야 한다.
- **백업 브랜치**: 이번 두 커밋에 대해 생성된 백업 브랜치는 없음(원격 최신 백업은
  `backup-2026-08-20-1755`). 문제가 발견되면 `git revert 2efa23d f4c81ad` 또는
  해당 커밋 이전(`891e7d8`)으로 개별 파일만 되돌리는 방식을 고려할 것.
- **직전 핸드오프 대비 이월/통합**: `2026-09-04-1552-캥거루코인합성.md`의
  "나머지 3개 히어로 슬라이드 교체 진행 여부 결정" 항목은 이번 세션에서 실행
  완료로 전환하되, "배포 사이트 렌더링 확인"과 "사용자 승인 확인" 항목으로
  대체해 이월함.
