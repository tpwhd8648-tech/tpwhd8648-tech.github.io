# 히어로 영상 시도 되돌림 및 카카오 아이콘 개편 인수인계 문서

작성일: 2026-08-07 (KST)

## 1. 목표

mx-dozn.co.kr 참고해 메인 히어로를 영상 단일 섹션으로 단순화하는 시도를 했으나
사용자가 기존 방식이 더 낫다고 판단해 되돌림. 이후 카카오톡 플로팅 버튼/소셜
아이콘이 실제 카카오톡 앱 아이콘과 다르게 생겼다는 문제를 발견해 모양(원형→
둥근사각형, "KAKAO"→"TALK")과 크기를 실제 아이콘에 맞게 개편함.

## 2. 현재 상태

### (1) 히어로 영상 단일 섹션 시도 → 되돌림 완료 (확인 완료)

- mx-dozn.co.kr을 web_fetch로 확인, 영상 1개 + 화살표/점 없는 미니멀 구조 참고
- 기존 6슬라이드 통합 히어로(영상 1개 + 이미지 슬라이드 5개)를 `.video-hero`
  (영상만, 화살표/점 없음) + `.hero-slider`(이미지 5개, 기존 화살표/점 유지)
  두 섹션으로 분리해 커밋·푸시(`41bb9d2`)
- 사용자가 "그전이 더 나은거같다"고 판단 → `git revert`로 되돌림(`1778e06`),
  푸시 완료. **최종적으로 원래 6슬라이드 통합 히어로 구조 그대로 유지 중**
- 되돌린 후 `node --check`, `inject-cache-busting.js` 재실행(0건 갱신)으로
  원상복구 정합성 확인함

### (2) 카카오 아이콘 모양 개편 (확인 완료 — node --check, cache-busting 검증)

- 사용자가 화면 우측 하단 카카오 플로팅 버튼이 실제 카카오톡 앱 아이콘과
  다르다고 지적, 스크린샷(실제 앱 아이콘: 둥근 사각형 + "TALK" 텍스트) 첨부
- `assets/js/footer.js` 한 파일에서 플로팅 버튼과 footer 소셜 아이콘 둘 다
  관리되는 구조 확인 → 한 번에 수정
  - 배경: `border-radius: 50%`(원형) → `30%`(둥근 사각형)
  - 텍스트: `KAKAO`(font-size 7) → `TALK`(font-size 9)
  - footer 소셜 아이콘(`.social-icon.kakao`)에도 `border-radius:30%` 인라인
    오버라이드 추가(`.social-icon` 공통 클래스가 원형 50%를 강제하므로)
- 커밋·푸시 완료(`da77107` → 최종 `a1dbc59`)

### (3) 카카오 플로팅 버튼 크기 축소 (확인 완료)

- 사용자 요청으로 플로팅 버튼만 살짝 축소: `54px` → `48px`, 내부 SVG
  `46px` → `40px` (footer 소셜 아이콘은 기존 40px 유지, 대상 아님)
- 커밋·푸시 완료(`3318d8d` → 최종 `04b317b`)

## 3. 진행 단계

이번 세션에서 모두 완료됨:

1. 브랜드명("OneTroy Bullion") 구조 분석 요청에 답변 (코드 변경 없음)
2. 할일 목록 정리: 최근 handoff 3건(1815/1838/2102) 교차 확인, 대분류 형식으로 제공
3. mx-dozn.co.kr web_fetch로 참고, 히어로 영상 단일화 계획 수립 → 사용자 확인
   (슬라이드 유지 범위/영상 소재/화살표 제거 여부 3가지 질문) → 답변 받고 진행
4. `index.html`/`assets/css/style.css` 수정, dead code(video 슬라이드 렌더 로직)
   정리, cache-busting 재실행, 커밋·푸시(`41bb9d2`)
5. 사용자가 이전 버전이 낫다고 판단 → `git revert`로 되돌리기, 충돌 없이
   자동 병합 확인 후 커밋·푸시(`1778e06`)
6. 사용자가 "카카오톡 풍선 따라다니는거 뭐였지" 질문 → 처음엔 index.html만
   검색해 못 찾음 → 재질문 받고 `assets/js/footer.js`에서 발견, 구조 설명
7. 사용자가 실제 카카오톡 아이콘 스크린샷 첨부, 차이점(원형↔사각형,
   KAKAO↔TALK) 확인 → footer.js 수정(플로팅 버튼 + 소셜 아이콘 동시)
   → 커밋·푸시(`da77107`)
8. 사용자가 크기 축소 요청 → 54px→48px 조정 → 커밋·푸시(`3318d8d`)

## 4. 다음에 진행할 일 (To-Do)

**이번 세션에서 새로 추가된 항목 없음.** 아래는 지난 세션(1838)과 동일한
목록이며, 이번 세션 작업(히어로 시도/되돌림, 카카오 아이콘 개편)은 모두
완결되어 To-Do에 남길 미결 사항이 없음.

### 🔍 SEO
- 🔴 👤 재제출한 sitemap.xml 처리 상태 재확인 (2026.8.3 재제출 기준, Search
  Console → Sitemaps에서 "마지막으로 읽은 날짜" 갱신 여부·발견 페이지 수 확인)
- 📢 👤 서치콘솔 오류 문구·크롤링 통계 추가 캡처해서 전달

### 🛍️ 상품 데이터
- 🔴 👤 정식 사업장 주소 확정 → 🤖 반영 (현재 봉익동 136은 랜드마크 표기일 뿐
  정식 주소 아님, 전자상거래법상 우선순위 높음)
- ⚪ 👤 소셜 미디어 계정 확정 → 🤖 footer.js 반영
- 🔵 👤 통신판매업신고번호 신청 여부 재검토 (거래량 50회 근접 시) — 신청 완료
  시 → 🤖 footer.js `898-26-02365` 뒤에 이어서 반영
- 🔵 👤 구매안내 탭 문구(결제/배송/교환·환불) 실제 정책 검토 후 → 🤖 반영

### 🎨 UI/UX
- 🟡 🤖 히어로 슬라이드 ↔ 카테고리 배너 이미지 중복 해소 (eagle/buffalo/kangaroo
  겹침) — 즉시 착수 가능 (단, 오늘 히어로 구조 되돌림으로 영향 없음, 여전히
  유효한 항목)
- 🔵 👤 카테고리 배너 실제 상품사진 교체 결과 확인
- 🔵 👤 about.html 디자인/문구 추가 보완 여부 결정

### ⚙️ 기능/인프라
- 🟡 👤 GA4 실시간 데이터 수집 확인 (모바일)
- 🟡 👤 간편 문의 폼(구글폼 연동) — 폼 생성 후 URL 전달 → 🤖 연동
- 🟡 👤 비밀번호 찾기(재설정) 기능 추가 여부 결정 → 🤖 반영
- 🔵 👤 favicon `.ico` / apple-touch-icon 추가
- 🔵 👤 네이버 톡톡 도입 여부 결정
- 🔵 👤 이메일 인증 도입 여부 결정

### 📋 운영/프로세스
- 🟡 👤 onetroybullion.com / .co.kr 실제 구매 진행 (보류 중)

**참고 (별도 레포 `tpwhd3141-cmyk`, premium-check.html 진단 도구, 선택
항목·낮은 우선순위)**:
- premium 값 과거 이력 시트 기록 검토 / localStorage → 서버 저장 전환 검토 /
  xaus.com·Frankfurter API 정상 동작 정기 확인

## 5. 참고 사항

- **백업 브랜치**: 이번 세션은 생성하지 않음 (단일/소수 파일 변경, 되돌리기도
  `git revert`로 간단히 처리 가능한 범위였음). 가장 최근 백업은
  `backup-2026-08-03-1829`(기준 커밋 `d0821c0`)로 여전히 유효.
- **히어로 구조는 최종적으로 변경 없음**: `41bb9d2`(분리) → `1778e06`(되돌림)
  두 커밋이 서로 상쇄되어, 현재 `main`의 `index.html`/`style.css` 히어로
  관련 코드는 세션 시작 시점과 동일함. 다음 세션에서 "히어로 영상화"를 다시
  시도할 계획이 있다면 이 두 커밋을 diff로 참고 가능.
- **새 히어로 영상 소재 관련**: 사용자가 추후 새 영상 파일을 준비하면
  `videos/hero-intro.mp4`를 교체하는 정도로 간단히 반영 가능(구조 변경 없이
  기존 슬라이드 1번 영상만 교체). 별도 To-Do로 등록하지 않음 — 사용자가 파일을
  준비했을 때 다시 언급할 사안.
- **카카오 아이콘 수정 범위**: `assets/js/footer.js` 한 파일에 플로팅
  버튼(JS로 동적 생성)과 footer 소셜 아이콘(HTML 템플릿 문자열) SVG가 각각
  독립적으로 정의돼 있어 두 곳 다 수정 필요했음(공유 컴포넌트 아님). 향후
  카카오 아이콘을 또 손볼 일이 있으면 이 두 위치를 함께 확인할 것.
- `pages/contact.html`, `pages/mypage.html`의 `.kakao-btn`(텍스트형 CTA
  버튼, "카카오톡으로 문의하기" 류)은 이번 수정 대상이 아니며 그대로 둠 —
  아이콘이 아니라 노란 배경의 텍스트 버튼이라 모양 이슈와 무관.
- **PAT 토큰**: 이번 세션 3회 push(히어로 되돌림, 카카오 모양 개편, 카카오
  크기 축소) 모두 채팅에 붙여넣어주신 동일 토큰 사용, 매번 push 직후 origin
  URL을 plain HTTPS로 원복함. 토큰 값은 응답에 노출하지 않았고 메모리/파일/
  로그에 저장하지 않음.
- 이전 핸드오프: `2026-08-04-2102-premiumcheck일별기록기능추가.md` (별도
  레포 작업), 메인 사이트 기준 직전 핸드오프는
  `2026-08-03-1838-deadCSS제거.md`
